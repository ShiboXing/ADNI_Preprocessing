PROJECT( IGNS_APPLICATIONS )

IF( IGNS_BUILD_BASIC )
    SUBDIRS( Basic )
ENDIF( IGNS_BUILD_BASIC )

IF( IGNS_BUILD_COMMON )
    SUBDIRS( Common )
ENDIF( IGNS_BUILD_COMMON )

IF( IGNS_BUILD_TRACKER )
    SUBDIRS( Tracker )
ENDIF( IGNS_BUILD_TRACKER )

IF( IGNS_BUILD_VIDEO_GRABBER )
    SUBDIRS( VideoGrabber )
ENDIF( IGNS_BUILD_VIDEO_GRABBER )

IF( IGNS_BUILD_IBIS )
    SUBDIRS( TrackUs2 )
ENDIF( IGNS_BUILD_IBIS )

IF( IGNS_BUILD_VOLUMIZESA )
    SUBDIRS( VolumizeStandAlone )
ENDIF( IGNS_BUILD_VOLUMIZESA )

IF( IGNS_BUILD_USCALIB )
    SUBDIRS( UScalib )
ENDIF( IGNS_BUILD_USCALIB )

IF( IGNS_BUILD_MINC_VIEW )
    SUBDIRS( MincView )
ENDIF( IGNS_BUILD_MINC_VIEW )

IF( IGNS_BUILD_VESSEL_SEGMENTATION )
    SUBDIRS( VesselSegmentation )
ENDIF( IGNS_BUILD_VESSEL_SEGMENTATION )

IF( IGNS_BUILD_GUI_VESSEL_SEGMENTATION )
    SUBDIRS( GUIVesselSegmentation )
ENDIF( IGNS_BUILD_GUI_VESSEL_SEGMENTATION )